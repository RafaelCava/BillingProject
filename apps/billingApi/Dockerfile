# This file is generated by Nx.
# Build the docker image with `npx nx docker:build @billing-management/billingApi`.
# Tip: Modify "docker:build" options in project.json to change docker build args.
#
# Run the container with `nx docker:run @billing-management/billingApi -p 3000:3000`.
#
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN npm add --global nx

CMD [ "tail", "-f", "/dev/null" ]
